node_modules/
data/
dist/
*.log
.DS_Store
puppeteer-cache/

# Worktrees isolados criados por agentes (Agent tool, isolation: "worktree")
.claude/worktrees/

# Banco de dados local e artefatos
*.db
*.db-shm
*.db-wal

# Legado (banco antigo do ContaFlow) — codigo permanece versionado
_legado/*.db
_legado/*.db-shm
_legado/*.db-wal

# Chave privada de licenciamento — NUNCA deve ser versionada (guarde uma copia
# segura fora do repositorio; sem ela nao da pra emitir novas licencas).
scripts/licenca/chave-privada.pem
scripts/licenca/licencas-emitidas.log
